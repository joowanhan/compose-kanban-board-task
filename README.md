# 🚀 2단계 - 칸반 보드 태스크(리팩터링)

# 기능 목록

비즈니스 로직 분리
단위 테스트 추가
UI 테스트 추가

## 기능 요구 사항

- [ ] 1단계 피드백 반영
    - [ ] Modifier
        - [ ] Modifier 주입
        - [x] Cadr의 Modifier에서 width()와 padding() 순서 변경
            - 디자인 시안이 요구하는 카드의 '전체 크기'를 알맞게 맞춘 구조
            - UI 컴포넌트를 설계할 때는 항상 **"전체 구조와 제약을 먼저 설정하고, 세부적인 내부 여백과 스타일을 나중에 깎아나간다"**는 규칙을 따라야 함
        - [ ] Named arguments 활용
            - 패딩 값에 positional argument가 사용된 것 등을 리팩토링
    - [ ] 명명 규칙
        - 다른 개발자가 이 함수명만 보고 "칸반 카드의 본문 텍스트"라는 것을 바로 알수 있게 함
    - [ ] PreviewParameter 활용
        - 다양한 상황(제목만, 칩만, 긴 텍스트 등)을 한 번에 확인할 수 있게 함
        - IDE에서 Preview가 잘 안보일 것
    - [ ] Divider 컴포넌트 사용
        - box로 구현되어 있던 것을 구분선에 대한 컴포넌트를 활용
- [ ] 비즈니스 로직과 UI 로직을 분리
    - [ ]
- [ ] 테스트 코드 추가
    - [ ] 단위 테스트
    - [ ] UI 테스트

## 프로그래밍 요구 사항

단위 테스트만으로도 충분한 로직과, UI 테스트가 필요한 영역을 구분한다.
핵심 비즈니스 로직을 가지는 객체를 분리해 단위 테스트를 진행한다.
Compose UI Testing을 활용하여 1단계의 기능 요구 사항을 테스트한다.